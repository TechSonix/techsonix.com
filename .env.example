# Optional: the existing public form identifier is retained by default.
# Web3Forms access keys are public form IDs; never put a private API token here.
NEXT_PUBLIC_WEB3FORMS_KEY=
# Set after the owner confirms a monitored PUBLIC contact address.
NEXT_PUBLIC_CONTACT_EMAIL=
NEXT_TELEMETRY_DISABLED=1
